# Handoff: Next Task is Issue #38

## Goal

Implement GitHub issue [#38: Add docs-content manifest and validation package](https://github.com/MyTuums-Group/MyTuums/issues/38).

Use the issue itself as the source of truth for requirements and acceptance criteria. This handoff only captures session-specific context and repo state that would otherwise be easy to miss.

## Current state

- Latest pushed commit on `main`: `95d0436` (`Clarify issue selection workflow`)
- That commit only updates the repo-local issue tracker guidance in `docs/agents/issue-tracker.md`
- No implementation work for `#38` has started yet
- `#38` is currently unassigned, labeled `ready-for-agent`, and its Kanban board status is `Ready`
- We chose `#38` as the best next task because it is a root unlocker for downstream docs work

## Downstream issues unlocked by #38

- [#39: Serve authorized docs index and pages through the API](https://github.com/MyTuums-Group/MyTuums/issues/39)
- [#42: Add generated docs search](https://github.com/MyTuums-Group/MyTuums/issues/42)
- [#43: Add read-only tldraw diagram support](https://github.com/MyTuums-Group/MyTuums/issues/43)
- [#44: Wire CI and deployment for docs](https://github.com/MyTuums-Group/MyTuums/issues/44)
- [#45: Seed initial manifest and developer docs navigation](https://github.com/MyTuums-Group/MyTuums/issues/45)

## Repo state to preserve

- There are still uncommitted local changes in `AGENTS.md` and `docs/agents/triage-labels.md`
- Those edits were intentionally left alone during this session
- Do not revert them or sweep them into a commit unless the user explicitly asks

## Useful repo context

- Existing app workspaces: `apps/api`, `apps/web`
- Existing packages: `packages/api-contract`, `packages/config`, `packages/db`, `packages/types`, `packages/ui`
- There is currently no `packages/docs-content`
- The repo-local issue tracker guidance was updated in `docs/agents/issue-tracker.md` so future triage should treat Kanban `Status` as the source of truth for "what should I do next?"

## Recommended starting moves

1. Read issue `#38` fully, then skim `#39` and `#45` so the generated artifact shape fits its first consumers.
2. Inspect workspace/package conventions under `packages/` before scaffolding `docs-content`.
3. Decide the manifest format and emitted artifact shape early, because validation, API serving, search generation, and navigation all depend on it.
4. Keep `docs/plans/**` excluded from the generated artifact from the start.

## Suggested skills

- No special skill is required to start.
- Use `zoom-out` if you want a quick repo-wide orientation before designing the package.
- Use `find-docs` only if you need current documentation for a specific library or tool choice.
- Per this repo's `AGENTS.md`, use the `ctx7` CLI whenever you need current library/framework/API documentation.
- Use `diagnose` only if the package wiring or workspace scripts start failing in a non-obvious way.
